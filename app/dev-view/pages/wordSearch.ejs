<%- contentFor('body') %>
<div class="headerSection">
    <div class="container">
        <div class="Word"><a href="/"> Word</a></div>
        <div class="logo">
                 <img src="/logo.svg">
              </div>
        <div class="searchBar">
                <input id="searchWord" type="text" placeholder="Type a word or a phrase to search">
                <button class="btn search-submit" type="button" onclick="search()"><span>Search</span></button>           
                </div>
        <div class="startingwithletter">
            <h1>List of words starting with letter <%= param.param.word %></h1>
			 <P class="NoResultsFound"> <%
			if(param.param.data[0].word == 'No results found, Similar word suggestions') {
			%>
			No results found, Similar word suggestions
			<%
			}
			%>
			</P>
				
            <ul>
                <%
				
                for(var k = 0; k < param.param.data.length;k++)
                {

					if(param.param.data[k].word !== 'No results found, Similar word suggestions') {
				%>
                <li><a href="/word/<%= param.param.data[k].word %>">
                        <%= param.param.data[k].word %>
                    </a></li>
                <%
				}
                }
                %>
            </ul>
			
			
        </div>
        <div class="Pagination">
<ul class="pagination">
<li>
    <% if(param.param.page > 1) {%>
    <a href="/search/<%= param.param.word%>/<%= parseInt(param.param.page)-1%>"> </a>
    <% } %>
</li>
    <%
    var startPage=1;
    var endPage=param.countresult.wcount/20;
    if(endPage > 10){
        endPage = 10;
    }
    if(param.param.page > 5){
        startPage=parseInt(param.param.page)-4;
        endPage=parseInt(param.param.page)+5;
    }
    if(endPage > param.countresult.lastpage){
        endPage = param.countresult.lastpage;
    }
            for(var i =startPage; i<=endPage;i++){

            if(i==param.param.page){
    %>
    <li class="active">
        <%
        }
        else{
        %>
    <li>
        <%
        }

        %>
        <a href="/search/<%= param.param.word%>/<%= i%>">
            <%= i%>
        </a>
    </li>
    <%  } %>
    <li>
        <% if(param.param.page < param.countresult.lastpage){%>
        <a href="/search/<%= param.param.word%>/<%= parseInt(param.param.page)+1%>">>></a>
        <% }%>
    </li>
</ul>


        </div>

    </div>
</div>
